import DraftCreateLayout from '@/components/Draf/DraftCreateLayout';
import useRequireAuth from '@/hooks/useRequireAuth';

export default function CreateDrafBerita() {
    return (
        <DraftCreateLayout />
    );
}